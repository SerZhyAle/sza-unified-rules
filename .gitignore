# Secrets and signing material - pre-empt the leak, never catch it afterwards.
*.pfx
*.snk
*.jks
*.keystore
*.p12
*.pem
*.cer
.env
*token*
*secret*

# Scratch
temp/
tmp/

# Build junk - deploy.ps1 stages everything, so this has to be excluded here or not at all.
__pycache__/
*.pyc
